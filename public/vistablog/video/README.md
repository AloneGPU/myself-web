# 视频资源（video）

与主题 **id 同名** 的 MP4，用于动态背景：

| 文件名 |
|--------|
| `forest-lake.mp4` |
| `misty-mountain.mp4` |
| `starry-peaks.mp4` |
| `sunrise-ocean.mp4` |
| `winter-dawn.mp4` |

视频加载失败时会自动退回 `bg/` 里的静态图。

不需要某主题的视频时：可删除文件，并在 `manifest.json` → `themeVideos` 中删掉对应 id。
