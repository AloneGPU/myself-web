import { useRef, useEffect, useCallback } from 'react'; export const useCounter = () => useRef(null); export const useCardAnimation = () => {}; export const useTitleAnimation = () => useRef(null); export const useScrollAnimation = () => {}; export const useHoverAnimation = () => ({ ref: useRef(null), onMouseEnter: () => {}, onMouseLeave: () => {} }); export const useRippleAnimation = () => ({ ref: useRef(null), onClick: () => {} }); export const useBounceAnimation = () => useRef(null); export const useSequenceAnimation = () => useRef(null); export const useMountAnimation = () => {}; export { default as anime } from 'animejs';
